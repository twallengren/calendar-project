org.gradle.caching=true
# Configuration cache is off: spotless keeps a JVM-local cache that goes stale under it
# (https://github.com/diffplug/spotless/issues/987) and fails with "lineEndingsPolicy".
org.gradle.configuration-cache=false
